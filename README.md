This Project maybe work only **linux debian base**

**Insatllation**

```bash
# Follow this command
./install.sh
docker compose up -d --build
make
hips -v
```

**Uninstall**

```bash
# Follow this command
make clean
```

**Detect Able**

1. SSH Brute Force

2. FTP Brute Force

3. Port Scan (Syn Scan, Null Scan, XMAS Scan, Full XMAS Scan)

4. DoS/DDoS (Syn Flood, ICMP Flood, UDP Flood)

5. Path Traversal (../../)

6. SQL Injection

7. XSS

8. Web Brute Force (HTTP Brute Force)